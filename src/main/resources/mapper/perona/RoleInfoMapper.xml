<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.guigui.perona.mapper.RoleInfoMapper">
    <resultMap type="RoleInfo" id="RoleInfoResult">
        <result property="roleId" column="role_id"/>
        <result property="roleName" column="role_name"/>
        <result property="roleKey" column="role_key"/>
        <result property="roleSort" column="role_sort"/>
        <result property="dataScope" column="data_scope"/>
        <result property="status" column="status"/>
        <result property="delFlag" column="del_flag"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="remark" column="remark"/>
    </resultMap>

    <resultMap type="RoleMenu" id="RoleMenuResult">
        <result property="roleId" column="role_id"/>
        <result property="menuId" column="menu_id"/>
    </resultMap>

    <sql id="roleInfoFields">role_id, role_name, role_key, role_sort, data_scope, status, del_flag, create_by, create_time, update_by, update_time, remark</sql>

    <sql id="selectRoleContactVo">
        select distinct r.role_id, r.role_name, r.role_key, r.role_sort, r.data_scope,
            r.status, r.del_flag, r.create_time, r.remark
        from role_info r
	        left join user_role ur on ur.role_id = r.role_id
	        left join user_info u on u.id = ur.user_id
    </sql>

    <select id="selectRoleInfoList" parameterType="RoleInfo" resultMap="RoleInfoResult">
        select
        <include refid="roleInfoFields"/>
        from role_info where del_flag = '0'
        <where>
            <if test="roleName != null  and roleName != ''">and role_name like concat('%', #{roleName}, '%')</if>
            <if test="roleKey != null  and roleKey != ''">and role_key = #{roleKey}</if>
            <if test="roleSort != null ">and role_sort = #{roleSort}</if>
            <if test="dataScope != null  and dataScope != ''">and data_scope = #{dataScope}</if>
            <if test="status != null  and status != ''">and status = #{status}</if>
            <if test="params.beginTime != null and params.beginTime != ''"><!-- 开始时间检索 -->
                and date_format(create_time,'%y%m%d') &gt;= date_format(#{params.beginTime},'%y%m%d')
            </if>
            <if test="params.endTime != null and params.endTime != ''"><!-- 结束时间检索 -->
                and date_format(create_time,'%y%m%d') &lt;= date_format(#{params.endTime},'%y%m%d')
            </if>
            <!-- 数据范围过滤 -->
            ${params.dataScope}
        </where>
    </select>

    <select id="selectRoleInfoById" parameterType="Long" resultMap="RoleInfoResult">
        select
        <include refid="roleInfoFields"/>
        from role_info
        where role_id = #{roleId}
    </select>

    <select id="selectRolesByUserId" parameterType="Long" resultMap="RoleInfoResult">
        <include refid="selectRoleContactVo"/>
        WHERE r.del_flag = '0' and ur.user_id = #{userId}
    </select>

    <select id="selectCountRoleMenuByMenuId" resultType="Integer">
	    select count(1) from role_menu where menu_id=#{menuId}
	</select>

    <select id="checkRoleNameUnique" parameterType="String" resultMap="RoleInfoResult">
        select
        <include refid="roleInfoFields"/>
        from role_info
        where role_name=#{roleName}
    </select>

    <select id="checkRoleKeyUnique" parameterType="String" resultMap="RoleInfoResult">
        select
        <include refid="roleInfoFields"/>
        from role_info
        where role_key=#{roleKey}
    </select>

    <insert id="insertRoleInfo" parameterType="RoleInfo" useGeneratedKeys="true" keyProperty="roleId">
        insert into role_info
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="roleName != null  and roleName != ''">role_name,</if>
            <if test="roleKey != null  and roleKey != ''">role_key,</if>
            <if test="roleSort != null ">role_sort,</if>
            <if test="dataScope != null  and dataScope != ''">data_scope,</if>
            <if test="status != null  and status != ''">status,</if>
            <if test="delFlag != null  and delFlag != ''">del_flag,</if>
            <if test="createBy != null  and createBy != ''">create_by,</if>
            <if test="createTime != null ">create_time,</if>
            <if test="updateBy != null  and updateBy != ''">update_by,</if>
            <if test="updateTime != null ">update_time,</if>
            <if test="remark != null  and remark != ''">remark,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="roleName != null  and roleName != ''">#{roleName},</if>
            <if test="roleKey != null  and roleKey != ''">#{roleKey},</if>
            <if test="roleSort != null ">#{roleSort},</if>
            <if test="dataScope != null  and dataScope != ''">#{dataScope},</if>
            <if test="status != null  and status != ''">#{status},</if>
            <if test="delFlag != null  and delFlag != ''">#{delFlag},</if>
            <if test="createBy != null  and createBy != ''">#{createBy},</if>
            <if test="createTime != null ">#{createTime},</if>
            <if test="updateBy != null  and updateBy != ''">#{updateBy},</if>
            <if test="updateTime != null ">#{updateTime},</if>
            <if test="remark != null  and remark != ''">#{remark},</if>
        </trim>
    </insert>

    <update id="updateRoleInfo" parameterType="RoleInfo">
        update role_info
        <trim prefix="SET" suffixOverrides=",">
            <if test="roleName != null  and roleName != ''">role_name = #{roleName},</if>
            <if test="roleKey != null  and roleKey != ''">role_key = #{roleKey},</if>
            <if test="roleSort != null ">role_sort = #{roleSort},</if>
            <if test="dataScope != null  and dataScope != ''">data_scope = #{dataScope},</if>
            <if test="status != null  and status != ''">status = #{status},</if>
            <if test="delFlag != null  and delFlag != ''">del_flag = #{delFlag},</if>
            <if test="createBy != null  and createBy != ''">create_by = #{createBy},</if>
            <if test="createTime != null ">create_time = #{createTime},</if>
            <if test="updateBy != null  and updateBy != ''">update_by = #{updateBy},</if>
            <if test="updateTime != null ">update_time = #{updateTime},</if>
            <if test="remark != null  and remark != ''">remark = #{remark},</if>
        </trim>
        where role_id = #{roleId}
    </update>

    <delete id="deleteRoleInfoById" parameterType="Long">
        delete from role_info where role_id = #{roleId}
    </delete>

    <delete id="deleteRoleInfoByIds" parameterType="String">
        delete from role_info where role_id in
        <foreach item="roleId" collection="array" open="(" separator="," close=")">
            #{roleId}
        </foreach>
    </delete>

    <delete id="deleteRoleMenuByRoleId" parameterType="Long">
		delete from role_menu where role_id=#{roleId}
	</delete>

    <delete id="deleteRoleDeptByRoleId" parameterType="Long">
		delete from role_dept where role_id=#{roleId}
	</delete>

    <insert id="batchRoleMenu">
        insert into role_menu(role_id, menu_id) values
        <foreach item="item" index="index" collection="list" separator=",">
            (#{item.roleId},#{item.menuId})
        </foreach>
    </insert>

    <insert id="batchRoleDept">
        insert into role_dept(role_id, dept_id) values
        <foreach item="item" index="index" collection="list" separator=",">
            (#{item.roleId},#{item.deptId})
        </foreach>
    </insert>

</mapper>